// Studio layout — wraps the embedded Sanity Studio.
// Kept minimal because Sanity Studio provides its own theming and layout.
export default function StudioLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
